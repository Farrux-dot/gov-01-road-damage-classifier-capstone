# GOV-01 Inference Artifacts

This folder will later document the final saved pothole-classification artifact. No final model has been trained or saved yet.

## Planned artifact contents

- Saved TensorFlow/Keras model.
- Image input rule: RGB image resized to 224 x 224 pixels.
- Preprocessing rule used by the selected model.
- Class mapping: `0 = Normal`, `1 = Pothole`.
- Dependency versions and minimal loading code.
- A known input image and expected prediction output for reload proof.

## Current status

Pending. The project will add real artifact paths and instructions only after a final candidate is selected using validation evidence.
